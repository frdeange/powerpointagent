[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = "--import-mode=importlib"
testpaths = [
    "services/pptx-mcp-server/tests",
    "services/image-mcp-server/tests",
    "services/orchestrator/tests",
]

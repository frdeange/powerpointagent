# requirements-dev.txt — root development tools
# Installed in the devcontainer for all contributors.
# Service-specific runtime deps live in services/*/requirements.txt

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0

# ── Linting & formatting ──────────────────────────────────────────────────────
black>=24.0.0
ruff>=0.4.0
mypy>=1.10.0

# ── HTTP client (for integration tests against local MCP servers) ─────────────
httpx>=0.27.0

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0.0
